<%- if controller_name != 'sessions' %>
  <%= link_to "ログイン", new_admin_user_session_path(@resource_name, class: "w-full flex items-center justify-center px-8 py-3 text-base border border-gray-800 font-medium rounded-md text-white text-gray-900 bg-gray-300 hover:bg-gray-400 md:py-4 md:text-lg md:px-10") %><br>
<% end %>

<%# - if @devise_mapping.registerable? && controller_name != 'registrations' %>
  <%# = link_to "新規登録", new_admin_user_registration_path(@resource_name), class: "w-full flex items-center justify-center px-8 py-3 text-base border border-gray-800 font-medium rounded-md text-white text-gray-900 bg-gray-300 hover:bg-gray-400 md:py-4 md:text-lg md:px-10" %><br>
<%# end %>

<%- if @devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <%= link_to "パスワード再発行", new_admin_user_password_path(@resource_name), class: "w-full flex items-center justify-center px-8 py-3 text-base border border-gray-800 font-medium rounded-md text-white text-gray-900 bg-gray-300 hover:bg-gray-400 md:py-4 md:text-lg md:px-10" %><br>
<% end %>
