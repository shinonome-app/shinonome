<%= form_with(model: kana_type) do |form| %>
  <% if kana_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kana_type.errors.count, "error") %> prohibited this kana_type from being saved:</h2>

      <ul>
        <% kana_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
