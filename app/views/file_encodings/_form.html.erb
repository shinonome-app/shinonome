<%= form_with(model: file_encoding) do |form| %>
  <% if file_encoding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file_encoding.errors.count, "error") %> prohibited this file_encoding from being saved:</h2>

      <ul>
        <% file_encoding.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
