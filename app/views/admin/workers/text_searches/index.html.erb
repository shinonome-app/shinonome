<div class="w-full md:w-2/3">
  <%= snm_headline h: 'h1' do %>工作員検索結果一覧<% end %>
  <%= render(Admin::WorkersTableComponent.new(workers: @workers, pagy: @pagy)) %>

  <div class="py-2">
  <%== pagy_snm_nav(@pagy) %>
  </div>
</div>
