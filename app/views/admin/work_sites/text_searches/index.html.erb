<div class="p-4 w-full bg-white ml-4 font-light mb-5 rounded">
  <h1 class="text-2xl">関連サイト検索結果一覧</h1>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>サイト名</th>
        <th>URL</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @sites.each do |site| %>
        <tr>
          <td><%= site.id %></td>
          <td><%= link_to site.name, admin_site_path(site) %></td>
          <td><%= site.url %></td>
          <td class="px-2">
            <%= form_with(model: [:admin, @work, @work.work_sites.build], method: 'post', data: { turbo: false }) do |form| %>
            <%= form.hidden_field :site_id, value: site.id %>
            <%= form.submit '関連づける', class: 'btn-admin-normal' %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <br>
</div>
