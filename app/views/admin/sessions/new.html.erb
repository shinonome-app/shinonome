<% if notice.present? %>
<snm-message-bar buttonStyle="blue"><%= notice %></snm-message-bar>
<% elsif alert.present? %>
<snm-message-bar buttonStyle="red"><%= alert %></snm-message-bar>
<% end %>

<h1 class="text-3xl text-white text-center mt-32 mb-16"><%= Rails.configuration.x.site_name %> DBへログインします</h1>
<div class="max-w-xl bg-white m-auto rounded-sm py-11 px-10 sm:px-28 mb-9">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: false }) do |f| %>
  <div class="mb-2">
    <%= f.label :login, class: "inline-block text-sm" %>
    <%= f.text_field :login, autofocus: true, required: true, class: "w-full border-2 border-ab_form focus:outline-none focus:border-ab_focus focus:ring-1 focus:ring-ab_focus focus:shadow-[0px_1px_13px_0px_#bee3f8] invalid:border-pink-500 invalid:text-pink-600 rounded outline-none transition duration-100 px-2 py-2" %>
  </div>
  <div class="mb-2">
    <%= f.label :password, class: "inline-block text-sm" %>
    <%= f.password_field :password, autocomplete: "current-password", required: true, class: "w-full border-2 border-ab_form focus:outline-none focus:border-ab_focus focus:ring-1 focus:ring-ab_focus focus:shadow-[0px_1px_13px_0px_#bee3f8] invalid:border-pink-500 invalid:text-pink-600 rounded outline-none transition duration-100 px-2 py-2" %>
  </div>
  <% if devise_mapping.rememberable? %>
  <div class="mb-2">
    <%= f.label :remember_me, class: "text-sm" do %>
    <%= f.check_box :remember_me %>
    ブラウザにログイン情報を保存
    <% end %>
  </div>
  <% end %>
  <div>
    <%= render(Admin::SubmitComponent.new(name: "commit", value: "ログイン")) %>
  </div>
  <% end %>
</div>
<p class="text-center text-base text-slate-200">
  編集作業を行うにはログインが必要です。<br>念のため、作業修了時にはログアウトを行ってください。<br>ログインするためには、クッキー機能が使用できる環境にしてください。
</p>
