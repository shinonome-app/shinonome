<div class="w-full">
  <%= snm_headline h: 'h2' do %>人物関連づけ登録<% end %>

  <div class="py-2">
    <p>対象人物: <%= @person.name %></p>
  </div>

  <%= render(Admin::PersonSearchComponent.new(form_url: admin_person_base_people_text_searches_path(person_id: @person.id))) %>

  <div class="mt-2 flex gap-2">
    <%= snm_link '戻る', href: admin_person_path(@person), button_style: "secondary" %>
  </div>
</div>
