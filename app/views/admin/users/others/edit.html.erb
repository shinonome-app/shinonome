<div class="w-full md:w-2/3">
  <snm-headline h="h1">管理者更新</snm-headline>
  <%= render 'form', user: @user %>
  <div class="flex">
    <%= tag.snm_link "戻る",  href: admin_users_others_path, buttonstyle: "secondary" %>
  </div>

  <snm-headline h="h1">管理者削除</snm-headline>

  <p>管理者を削除します。この操作は戻せません。</p>

  <div class="flex mt-5">
    <%= button_to '削除', admin_users_other_path(@user), method: :delete, form: { data: { turbo_confirm: '本当に削除しますか?' } }, class: delete_button_class %>
  </div>

</div>
