<div class="p-4 w-full bg-white ml-4 font-light mb-5 rounded">
<p id="notice"><%= notice %></p>
<h1 class="text-2xl">Web入力受付管理</h1>

<p>
  <%= @receipts.count %>件の作品が見つかりました。　　　
</p>
<table>
  <thead>
    <tr>
      <th>連番</th>
      <th>作品名</th>
      <th>文字遣い</th>
      <th>著者</th>
      <th>入力者</th>
      <th>登録</th>
    </tr>
  </thead>
  <tbody>
    <% @receipts.each do |receipt| %>
      <tr>
        <td><%= link_to receipt.id, edit_admin_receipt_path(receipt) %></td>
        <td><%= link_to receipt.title, edit_admin_receipt_path(receipt) %></td>
        <td><%= receipt.kana_type_id %></td>
        <td><%= receipt.person_id %></td>
        <td><%= receipt.worker_name %></td>
        <td><%= receipt.register_status %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>
