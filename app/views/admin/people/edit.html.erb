<div class="w-full md:w-2/3">
  <snm-headline h="h1">人物更新</snm-headline>

  <%= render(Admin::PersonFormComponent.new(person: @person)) %>

  <div class="mt-5 flex">
    <%= tag.snm_link '戻る', href: admin_person_path(@person), buttonstyle: "secondary" %>
  </div>
</div>
