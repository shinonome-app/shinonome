<%= form_with(model: bibclass) do |form| %>
  <% if bibclass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bibclass.errors.count, "error") %> prohibited this bibclass from being saved:</h2>

      <ul>
        <% bibclass.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.number_field :book_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :num %>
    <%= form.text_area :num %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
