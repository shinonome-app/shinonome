<div class="justify-center items-center bg-blue-50 content-center w-screen">
  <div class="px-8 max-w-6xl mx-auto">
    <a name="top">&nbsp;</a>

    <div class="flex justify-end text-base">
      ●<a href="https://www.aozora.gr.jp/" class="link-normal">トップ</a>　●<a href="/" class="link-normal">作業着手連絡システム</a>　●<span class="text-red-500 font-semibold">校正受付システム</span>
    </div>

    <h1 class="flex justify-center py-3 text-2xl font-semibold bg-yellow-100 border-t border-b border-gray-500">校正受付システム　作家リスト：<%= @kana %></h1>

    <h2 class="flex my-4 px-5 py-1 text-2xl font-semibold bg-gray-300 border-t border-b border-gray-500"><%= @kana %></h2>

    <div class="px-12 py-5">
      <ol class="list-decimal">
        <% @authors.each do |person| %>
        <% next if person.works.not_proofread.count <= 0 %>
        <li>
          <%= link_to person.name, proofreads_person_path(person), class: "text-blue-700 hover:text-gray-100 hover:bg-blue-700 visited:text-purple-600 underline" %>
          (校正待ち：<%= person.works.not_proofread.count %>)　　<% if person.copyright_flag? %><strong>＊著作権存続＊</strong>　<% end %>
        </li>
        <% end %>
      </ol>
    </div>

    <div class="text-right py-4">
      <a class="text-blue-700 hover:text-gray-100 hover:bg-blue-700 visited:text-purple-600 underline" href="#top">▲</a>
    </div>

    <hr class="border-gray-400">

  </div>
</div>
