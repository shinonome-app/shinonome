<%= form_with(model: charset) do |form| %>
  <% if charset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(charset.errors.count, "error") %> prohibited this charset from being saved:</h2>

      <ul>
        <% charset.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
